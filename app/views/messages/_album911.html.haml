.album
  .album_top
    .album_top_title
      = @group.name
      の9ヶ月から11ヶ月までのアルバム
      .album_editbtn
        %button.album_edit.btn 編集
  .album_contents
    .album_main
      .album_main_content
        .album_main_content_img
        - if @group.stand?
          %img.album_photo{:src => @group.stand}/
        - else
          .none_photo
            %img.album_none{:src => '/errer/akatyan02.png'}/
      .album_main_content
        .album_main_content_img
        - if @group.high3?
          %img.album_photo{:src => @group.high3}/
        - else
          .none_photo
            %img.album_none{:src => '/errer/akatyan02.png'}/
    .album_main
      .album_main_title
        初めてのたっち
        .album_updatebtn.hide
          = form_for(@group, local: true) do |f|
            = f.label :album_img, class: "album_update" do
              = f.text_field :stand, placeholder: "Image Url"
              = f.submit '送信', class: 'album_update, btn'
      .album_main_title
        今日もハイハイどこまでも
        .album_updatebtn.hide
          = form_for(@group, local: true) do |f|
            = f.label :album_img, class: "album_update" do
              = f.text_field :high3, placeholder: "Image Url"
              = f.submit '送信', class: 'album_update, btn'

    .album_main
      .album_main_content
        .album_main_content_img
        - if @group.sleepingface3?
          %img.album_photo{:src => @group.sleepingface3}/
        - else
          .none_photo
            %img.album_none{:src => '/errer/akatyan02.png'}/
      .album_main_content
        .album_main_content_img
        - if @group.saycheese3?
          %img.album_photo{:src => @group.saycheese3}/
        - else
          .none_photo
            %img.album_none{:src => '/errer/akatyan02.png'}/
    .album_main
      .album_main_title
        可愛い寝顔
        .album_updatebtn.hide
          = form_for(@group, local: true) do |f|
            = f.label :album_img, class: "album_update" do
              = f.text_field :sleepingface3, placeholder: "Image Url"
              = f.submit '送信', class: 'album_update, btn'
      .album_main_title
        こっちむーいて
        .album_updatebtn.hide
          = form_for(@group, local: true) do |f|
            = f.label :album_img, class: "album_update" do
              = f.text_field :saycheese3, placeholder: "Image Url"
              = f.submit '送信', class: 'album_update, btn'

    .album_main
      .album_main_content
        .album_main_content_img
        - if @group.imitate?
          %img.album_photo{:src => @group.imitate}/
        - else
          .none_photo
            %img.album_none{:src => '/errer/akatyan02.png'}/
      .album_main_content
        .album_main_content_img
        - if @group.go?
          %img.album_photo{:src => @group.go}/
        - else
          .none_photo
            %img.album_none{:src => '/errer/akatyan02.png'}/
    .album_main
      .album_main_title
        まねっこまねっこ
        .album_updatebtn.hide
          = form_for(@group, local: true) do |f|
            = f.label :album_img, class: "album_update" do
              = f.text_field :imitate, placeholder: "Image Url"
              = f.submit '送信', class: 'album_update, btn'
      .album_main_title
        パパを乗り越えママの元へ
        .album_updatebtn.hide
          = form_for(@group, local: true) do |f|
            = f.label :album_img, class: "album_update" do
              = f.text_field :go, placeholder: "Image Url"
              = f.submit '送信', class: 'album_update, btn'

    .album_main
      .album_main_content
        .album_main_content_img
        - if @group.hair?
          %img.album_photo{:src => @group.hair}/
        - else
          .none_photo
            %img.album_none{:src => '/errer/akatyan02.png'}/
      .album_main_content
        .album_main_content_img
        - if @group.straw?
          %img.album_photo{:src => @group.straw}/
        - else
          .none_photo
            %img.album_none{:src => '/errer/akatyan02.png'}/
    .album_main
      .album_main_title
        今日はヘアカットの日
        .album_updatebtn.hide
          = form_for(@group, local: true) do |f|
            = f.label :album_img, class: "album_update" do
              = f.text_field :hair, placeholder: "Image Url"
              = f.submit '送信', class: 'album_update, btn'
      .album_main_title
        ストロー飲みデビュー
        .album_updatebtn.hide
          = form_for(@group, local: true) do |f|
            = f.label :album_img, class: "album_update" do
              = f.text_field :straw, placeholder: "Image Url"
              = f.submit '送信', class: 'album_update, btn'

